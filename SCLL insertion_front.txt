#include <stdio.h>
#include <stdlib.h>
struct node 
{
	int in;

	struct node* next;
};
struct node* last = NULL;
	struct node* temp;
	temp = (struct node*)malloc(sizeof(struct node));
	if (last == NULL) 
	{
		temp->in = data;
		temp->next = temp;
\	
	}
	else 
	{
		temp->in = data;
		temp->next = last->next;
		last->next = temp;
	}
	insertAtFront(30);
	viewList();
	return 0;
}
